var Reflux = require('reflux'),
  React = require('react'),
  placesStore = require('../stores/places'),
  Defs = require('./defs');

module.exports = React.createClass({
  render: function() {
    return (
      <g>

      </g>
    );
  }
});