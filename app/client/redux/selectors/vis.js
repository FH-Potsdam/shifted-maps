export default state => state.vis;

export const visScaleSelector = state => state.vis.scale;

export const visBoundsSelector = state => state.vis.bounds;

export const visViewSelector = state => state.vis.view;