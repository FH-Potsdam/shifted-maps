import styled from 'styled-components';

export const Icon = styled.svg`
  fill: currentColor;
  width: 1em;
  height: 1em;
  vertical-align: -3px;
`;

export const StrokeIcon = styled.svg`
  stroke-width: 3px;
  stroke: currentColor;
`;

export const Logo = styled.svg`
  fill: currentColor;
`;
